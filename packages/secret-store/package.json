{
  "name": "@nisse/secret-store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "pnpm run build && node --test test/secret-store.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
